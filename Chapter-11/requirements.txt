# Core LangChain / LangGraph stack (a tested, mutually-compatible set).
# Note: langgraph 0.2.76+ is required for langchain-core 0.3.x — the older
# langgraph 0.2.0 pins langchain-core <0.3 and will not resolve.
langchain==0.3.30
langchain-community==0.3.31
langchain-core==0.3.86
langchain-text-splitters==0.3.11
langgraph==0.2.76
langchain-ollama==0.2.3

# Local vector search
faiss-cpu==1.14.3         # use faiss-gpu on an NVIDIA host for acceleration

# Document loading
pypdf==5.9.0
python-docx==1.2.0
beautifulsoup4==4.15.0
lxml==5.4.0

# Config, validation
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# Serving
fastapi==0.115.14
uvicorn[standard]==0.30.6
httpx==0.27.2
python-multipart==0.0.32   # required by FastAPI for file uploads / form fields

# Testing
pytest==8.4.2

# ── Optional extras (uncomment as needed) ────────────────────────────────
# Cloud-scale vector search (Section 4.2):
# langchain-pinecone>=0.2
# pinecone-client>=5
#
# Better PDF layout handling + OCR fallback (Section III):
# pdfminer.six>=20240706
# pytesseract>=0.3.10        # also install the Tesseract binary on your system
# pdf2image>=1.17.0          # also install poppler
