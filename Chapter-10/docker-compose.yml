# Local stack for the AI Workflow Designer: Ollama (model server) + the API.
#
# Quick start:
#   docker compose up -d ollama
#   docker compose exec ollama ollama pull llama3.1:8b
#   docker compose exec ollama ollama pull llama3.1:70b   # large; needs a capable GPU/host
#   docker compose up app
#
# The app talks to Ollama over the compose network at http://ollama:11434.
services:
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    # Uncomment to expose host GPUs (NVIDIA):
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]

  app:
    build: .
    depends_on:
      - ollama
    environment:
      OLLAMA_BASE_URL: http://ollama:11434
      CHECKPOINT_DB_PATH: /app/data/conversations.db
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    command: uvicorn src.api:app --host 0.0.0.0 --port 8080

volumes:
  ollama_models:
